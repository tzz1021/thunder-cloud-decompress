不能三秒盲扫文件夹，动不动一天流量上百个请求，三秒看似发了一个实际上是发了100+请求
即使同一个ip，同一个浏览器也不行的.之前还把openlist转发到了公网{可笑}

fix：Gemini
两次扫描机制，精确到每一个文件夹【秒传检测一次，10min后间隔十秒检测一次】
不能一次性盲扫100+必须精确到每一个文件夹


openclaw：改好了，核心变化：

1. **Alist API 调用间隔从 3 秒 → 45 秒**，加了全局 `_LAST_ALIST_CHECK` 限频
2. **没有 downloading 任务时彻底不调 API**（重置计时器）
3. **下载进度检查按需执行**，不每次循环都走

这样每分钟最多 1-2 次 API 调用，不至于把 OpenList 扫崩触发 2FA。

重启主循环试试看，应该不会再反复弹 2FA 了。
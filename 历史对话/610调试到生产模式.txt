接下来进入生产模式了，请拷贝v4的所有文件到v5,然后按照下面的说明进行生产预装。
（老样子不明白尽管问，有建议尽管提，清楚完美大胆改，可以变成我的性格或者长期记忆之类的。我就不在多说了）

*1*每一次点击都要有容错机制，不能因为任何一次点击卡主直接死翘翘，日志输出直接输出一样的即可，输出了几次就是尝试了几次（同一个步骤最多尝试三次）
特别的，尝试新建文件夹为了避免冲突需要改重命名机制，后缀（1）（2），现在不用给这一步添加容错机制，因为从来没有在这里出现差错
*2*一次debug结束之后直接模拟F5即可回到target_dir，然后开始读取os传来的信息，判断接下来是 给下一个用户添加 or 给已经添加好的用户解压分享（包括重命名等一系列琐碎步骤）
*3*保持step1-4的所有逻辑不改变仅增加容错性尝试，step5不变，确保可以继续用于测试

真正的联动应该是
新建一个数据库sqlite（或者有什么更好的方案）用来存储待添加列队，待解压分享列队，返回前端的分享列队
安装好第三方库顺手的事。对于每一个用户而言，前端应该实时显示在这个用户之前还有多少人待添加，多少人待解压分享

现在怎么搞，
写一个step6允许json格式数据解析，模拟一系列用户发出请求，一个请求总计三个变量，填写时间、排队序号和直链url。（这里排队序号按照填写时间先后由算法生成不是人工填写）
step6的返回结果应该是另外一个json数据，一条信息总计两个，排队序号（就是上面的排队序号）和分享链接（对win32api得到的数据正则匹配只保留http开头井号结尾的部分）
暂时不用数据库，我写一个模拟的用户请求（一个json文件）看返回结果，因为需要找各种来源各种大小的文件直链url，这比数据库修改起来方便


1.selenium自动化分成这样的几部分：单击（左键右键），悬停，填写，寻找“各种元素”。对于单击和寻找“各种元素”都需要有容错机制，悬停和填写因为没出过错暂时不需要。封装成一个单独的函数是更优解
2.并不是，这里的os返回值应该是一个变量，请优先查看现有变量寻找与之接近的现有变量，该返回值是监听到的发生“修改时间变化”的文件夹列队（按照用户发送的时间确定顺序）（显然dict2里面的代表已经归档的文件夹不可能存在于该列队）
列队空的那么“给下一个用户添加”  列队非空那么选择列队里的第一个文件夹“给已经添加好的用户解压分享（包括重命名等一系列琐碎步骤）”
这里最好使用数据库查询表示，dict2已经有点捉襟见肘了，或者有什么更好的建议也可以提出
3.step6可以参考step5但是要求即使删除step5，只有12346也可以在长期运行的debug模式浏览器（9222端口）实现从输入数据转变成输出数据的功能

放在本地是更优解，我可以随时查询进度，而且用户之间完美隔离，
服务器存储数据库不仅会被服务器供应方看到还有可能被用户黑进去拿到其他用户的信息，这毫无隐私可言，
如果你愿意现在就可以不再依赖json解析模拟生产环境，直接把前端在本地5000搭起来，我开好多窗口发请求代替大量用户发请求，在这样的生产环境更加真实
这样的step6需要的功能是否清晰，如何修正step1-4是否清晰？


